{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute badminton-db --local --file=src/db/schema.sql",
    "db:migrate:prod": "wrangler d1 execute badminton-db --remote --file=src/db/schema.sql"
  },
  "dependencies": {
    "better-auth": "^1.2.7",
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250409.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.5.1"
  }
}
